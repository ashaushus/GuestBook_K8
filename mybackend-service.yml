apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    name: backend-svc
    app: guestbook-app
  spec:
      type: LoadBalancer
      ports:
      - port: 80
        targetPort: 80
      selector:
        name: backend-pod
        app: guestbook-app